fn main() {
    // TODO: Change the line below to fix the compiler error.
    let x = 10;
    // You can type annotate the variable by adding : i32 after the variable name.

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
